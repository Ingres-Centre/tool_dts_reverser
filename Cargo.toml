[package]
name = "dts-reverser"
version = "0.2.0"
edition = "2024"

[dependencies]
lalrpop-util = {version = "*", features = ["lexer"]}
logos = {version = "*", features = ["logos-derive"]}
bimap = "*"

[build-dependencies]
lalrpop = "*"